# WMS-SAEP-v2 overview
- Django project scaffold on Python 3.13.
- Current stack: Django 6.0.x, SQLite for local default database, standard Django project layout.
- Current repository shape: `manage.py`, `config/` (`settings.py`, `urls.py`, `asgi.py`, `wsgi.py`), `pyproject.toml`, `uv.lock`.
- No domain apps or tests are present yet in the repository root scan.
- Project is being worked on in `/Users/jmzr/Dev/WMS-SAEP-v2` on Darwin/macOS.